export interface Project {
	title: string;
	client: string;
	description: string;
	publishDate: string;
	tags: string[];
	img: string;
}

export interface Service {
	title: string;
	description: string;
	img: string;
	gallery: string[];
}
